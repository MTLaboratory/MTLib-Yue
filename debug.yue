_G["mtlib"] = require([[mtlib]])

serial = mtlib.string.serialize

_G["explore"] =(what)-> print(serial(what))

print(serial(mtlib._NAME) .. serial(mtlib._VERSION) .. serial(mtlib._VERSION_NAME))
print("\ttype `cont` to exit the Lua debugger.")

debug.debug!

return nil